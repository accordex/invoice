# Database
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/invoice_db?schema=public"

# API
API_PORT=3001
JWT_SECRET="change-me-in-production-use-long-random-string"
JWT_EXPIRES_IN="7d"

# Web (Vite) — leave empty in production (same-origin /api)
VITE_API_URL=""
